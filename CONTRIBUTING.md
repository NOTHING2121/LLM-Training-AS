# Contributing

## Setup

Install the project dependencies before making changes:

```bash
pip install -r requirements.txt
```

## Development

- Keep changes focused and document behavior that is not self-explanatory.
- Add or update tests when changing model, dataset, or decoding behavior.
- Run the relevant tests before opening a pull request.

```bash
pytest tests
```

Use clear commit messages that describe the purpose of each change.
